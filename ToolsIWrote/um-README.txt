Copy `um` to a place like /usr/bin
or /home/<name>/.local/bin
and run `chmod +x <PATH>/um`.

Now, you can easily bring up a google search
on your computer from your terminal by typing:

```
um Python command line argument tools
```
